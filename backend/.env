# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_NAME=mini_competition_db
DB_USER=postgres
DB_PASSWORD=postgres

# Judge Service Configuration
JUDGE_HOST=mini-judge
JUDGE_PORT=3000

# JWT Configuration
# IMPORTANT: Generate a strong secret with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_SECRET_IN_PRODUCTION
JWT_EXPIRATION=3600

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
